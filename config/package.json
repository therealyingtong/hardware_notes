{
    "name": "su-config",
    "version": "1.0.0",
    "main": "build/index.js",
    "scripts": {
        "build": "tsc && node build/export-config.js > ../cli/exported_config.json",
        "build-kovan": "tsc && NODE_ENV=kovan-dev node build/export-config.js > ../cli/exported_config.json",
        "watch": "tsc --watch"
    },
    "author": "Ying Tong",
    "license": "GPL-3.0-or-later",
    "dependencies": {
        "config": "^3.3.8",
        "js-yaml": "^3.13.1",
		"path": "^0.12.7"
    },
    "devDependencies": {
        "@types/node": "^12.0.10"
    }
}
